MemoryQuestion actions. Reply with the returned value ONLY - no narration.

count     {"action":"count","game_map":[[...]],"question":"how many c7?"} -> {"answer":"4"}
          Output "answer". Never count tiles yourself.
store     {"action":"store","key":"<name>","value":"<data>"}
retrieve  {"action":"retrieve","key":"<name>"}
transform {"action":"transform","key":"<name>","rule":"<rule>"} -> {"answer":"<result>"}
          first2last2 = first 2 + last 2 chars      ("ABCDEF"   -> "ABEF")
          char5char7  = 5th + 7th char, 1-indexed   ("ABCDEFGH" -> "EG")

Doors: use transform, not retrieve, so the tool computes the answer.
  grey key/door   -> key="door_key_c32", rule="first2last2"
  yellow key/door -> key="door_key_c33", rule="char5char7"

CRITICAL: never count, index, or slice characters yourself, and never re-check or
"correct" the tool's answer - output the "answer" field verbatim. If a call returns
success:false the key was never stored; do not invent an answer.
